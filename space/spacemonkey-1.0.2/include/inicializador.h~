#ifndef INICIALIZADOR_SDL_E_OUTRAS_BAGACAS
#define INICIALIZADOR_SDL_E_OUTRAS_BAGACAS

#include"initException.h"

class Inicializador
{
public:
	void initGrafico()throw(InitException);
	void initSom()throw(InitException);
};

#endif
